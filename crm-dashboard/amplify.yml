version: 1
frontend:
  phases:
    preBuild:
      commands:
        - cd crm-dashboard
        - npm install
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: crm-dashboard/dist
    files:
      - '**/*'
  cache:
    paths:
      - crm-dashboard/node_modules/**/*